				</div>
			</div>
			<div class="card{if $element->name} card-underline{/if}">
			{if $element->name}
				<div class="card-head">
					<header>{$element->name}</header>
				</div>
			{/if}
				<div class="card-body main">